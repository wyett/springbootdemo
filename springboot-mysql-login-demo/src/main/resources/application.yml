server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    username: wyett_rw
    password: jumpjump
    url: jdbc:mysql://localhost:3306/wyett?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect